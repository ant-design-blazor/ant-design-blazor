@namespace AntDesign
@inherits ColumnBase

@if (IsPlaceholder)
{
    <td style="padding: 0px; border: 0px; height: 0px;"></td>
}
else if (IsColGroup)
{
    if (Width > 0)
    {
        <col style="width: @(Width)px; min-width: @(Width)px;">
    }
}
else if (IsHeader)
{
    <th class="@ClassMapper.Class ant-table-selection-column" style="@FixedStyle @Style">
        @if (Type == "checkbox")
        {
            <Checkbox @bind-Checked="Checked" CheckedChange="HandleCheckedChange" Indeterminate="Indeterminate" /> @Index
        }
    </th>
}
else
{
    <td class="@ClassMapper.Class ant-table-selection-column" style="@FixedStyle @Style">
        @if (Type == "checkbox")
        {
            <Checkbox @bind-Checked="Checked" Disabled="Disabled" CheckedChange="HandleCheckedChange" />
        }
        else if (Type == "radio")
        {
            <Radio @bind-Checked="Checked" Disabled="Disabled" CheckedChange="HandleCheckedChange" />
        }
        @Index
    </td>
}