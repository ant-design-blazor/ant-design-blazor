@using System.ComponentModel.DataAnnotations;

<Form Model="@model1"
      LabelColSpan="8"
      WrapperColSpan="16"
      Validator="validator">
    <FormItem Label="Number 0~10">
        <Input type="number" @bind-Value="@context.Number"/>
    </FormItem>
</Form>
<Form Model="@model2"
      LabelColSpan="8"
      WrapperColSpan="16">
    <Validator>
        <CustomValidator/>
    </Validator>
    <ChildContent>
        <FormItem Label="Number 10~20">
            <Input type="number" @bind-Value="@context.Number"/>
        </FormItem>
    </ChildContent>
</Form>

@code
{
    public class Model1
    {
        [Range(0, 10)]
        public string Number { get; set; }
    }

    public class Model2
    {
        [Range(10, 20)]
        public string Number { get; set; }
    }

    private Model1 model1 = new Model1();

    private Model2 model2 = new Model2();

    private RenderFragment validator = @<CustomValidator/>;
}
