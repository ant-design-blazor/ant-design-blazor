<Form>
    <FormItem Label="名字"
              Name="Name"
              Rules="@(new FormRule[] { new FormRule { Required = true, Message = "Please input your Name!" } })">
        <DatePicker />
    </FormItem>

    <button type="submit">Submit</button>
</Form>

@code
{
    public class Model
    {
        public string Name { get; set; }
        public int Age { get; set; }
    }

    private Model model = new Model();
}