---
order: 5
title:
  zh-CN: 自定义高亮区域
  en-US: Custom highlighted area
---

## zh-CN

使用 `GapOffset` 控制高亮区域与目标元素之间的间距，使用 `GapRadius` 控制圆角。

## en-US

Use `GapOffset` to control the spacing around the highlighted area and `GapRadius` to control its corner radius.
