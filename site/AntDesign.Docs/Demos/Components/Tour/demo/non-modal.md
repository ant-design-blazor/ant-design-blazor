---
order: 1
title:
  zh-CN: 非模态
  en-US: Non-modal
---

## zh-CN

使用 `Mask="false"` 可以将引导变为非模态，同时建议与 `Type="TourType.Primary"` 组合使用。

## en-US

Use `Mask="false"` to make Tour non-modal. It is recommended to combine it with `TourType.Primary`.
