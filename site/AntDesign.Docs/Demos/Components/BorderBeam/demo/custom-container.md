---
order: 3
title:
  zh-CN: 自定义容器
  en-US: Custom container
---

## zh-CN

自定义容器也可以作为 `BorderBeam` 的宿主。宿主元素需要提供定位上下文，通常设置 `position: relative` 即可。

## en-US

A custom container can also host `BorderBeam`. The host element needs a positioning context, usually provided with `position: relative`.
