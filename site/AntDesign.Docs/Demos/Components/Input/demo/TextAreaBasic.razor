<TextArea Rows="4" OnPressEnter="OnPressEnter" Placeholder="Press Ctrl + Enter to submit" />

@inject IMessageService message
@code {
    private void OnPressEnter(KeyboardEventArgs e)
    {
        if (e.CtrlKey)
        {
            message.Info("Ctrl + Enter");
        }
    }
}