---
order: 0
title:
  zh-CN: 基本使用
  en-US: Basic Usage
---

## zh-CN

最基本的用法，自动从泛型类型 `TItem` 解析属性并生成筛选条件。支持字符串、数字、日期、枚举等类型。

## en-US

Basic usage. Auto-resolves properties from the generic type `TItem` and generates filter conditions. Supports string, number, date, enum, and other types.